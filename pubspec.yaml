name: admin_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_secure_storage: ^9.2.2
  get: ^4.6.6
  responsive_fonts: ^0.0.3
  http: ^1.2.1
  crypto: ^3.0.3
  encrypt: ^5.0.3
  flutter_form_builder: ^9.3.0
  datetime_setting: ^2.0.2
  image_picker: ^1.1.2
  dart_jsonwebtoken: ^2.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
